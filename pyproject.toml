[project]
name = "yinxi"
version = "0.1.0"
description = "An open-source AI coding agent for the terminal"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.28.0",
    "rich>=14.3.3",
]

[project.scripts]
yinxi = "yinxi.cli:main"
